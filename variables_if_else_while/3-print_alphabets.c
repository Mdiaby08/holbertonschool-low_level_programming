#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char c; char C;

for (c = 'a'; c <= 'z'; C = 'A'; C <= 'Z'; c++, C++ )
putchar(c);

putchar('\n');

return (0);
}
