#include "main.h"

/**
* _islower - checks for lowercase character
*
*Returns: 1 lowercase, 0 otherwise
*/

int _isalpha(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
if else (c >= 'A' && c <= 'Z')
else
return (0);
}
