#include "main.h"

/**
* _islower - checks for lowercase character
*
*Returns: 1 lowercase
*Returns: 0 otherwise 
*/
int _islower(int c);
int _putchar(char c);
{
int c;
if (c >= 'a' && 'z'<= c);
{
_putchar (c)
return (1)
}
else
{
_putchar (c)
}

return (0)
}
